<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mobaas.stfx.dao.mybatis.SysPermissionMapper">

    <select id="findListByRoleId" resultType="com.mobaas.stfx.entity.SysPermission">
        SELECT
        	perm.id,
            perm.`name`,
            perm.permission,
            perm.url
        FROM
            sys_permission perm
        INNER JOIN sys_role_permission role_perm ON role_perm.permissionId = perm.id
        INNER JOIN sys_role role ON role.id = role_perm.roleId
        WHERE role.id=#{roleId}
    </select>
    <select id="findList" resultType="com.mobaas.stfx.entity.SysPermission">
        SELECT
        	perm.id,
            perm.`name`,
            perm.permission,
            perm.url
        FROM
            sys_permission perm
    </select>
</mapper>